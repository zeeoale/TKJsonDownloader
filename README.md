# TK JSON Downloader

Simple GUI to browse and download worlds (JSON + preview) from https://json.tikey.art/index.json

## Run (dev)
python3 tk_json_downloader.py

## Builds
GitHub Actions builds Linux + Windows with PyInstaller.
Tag a release like `v1.0.0` to publish binaries in GitHub Releases.
